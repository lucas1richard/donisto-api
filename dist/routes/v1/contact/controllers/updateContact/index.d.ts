import { Request, Response, NextFunction } from 'express';
declare const updateContactController: (req: Request, res: Response, next: NextFunction) => Promise<void>;
export default updateContactController;
