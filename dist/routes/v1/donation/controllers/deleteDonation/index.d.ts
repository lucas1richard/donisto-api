import { Request, Response, NextFunction } from 'express';
declare const deleteDonationController: (req: Request, res: Response, next: NextFunction) => Promise<void>;
export default deleteDonationController;
