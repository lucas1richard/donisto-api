import Sequelize from 'sequelize';
import {{ properCase name }}Keys from '../types/{{ properCase name }}Keys';

const attributes = {
  id: {
    type: Sequelize.BIGINT,
    autoIncrement: true
  },
  [{{ properCase name }}Keys.UUID]: {
    type: Sequelize.UUID,
    defaultValue: Sequelize.UUIDV4,
    primaryKey: true
  }
};

export default attributes;
