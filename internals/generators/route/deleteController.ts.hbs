import { Request, Response, NextFunction } from 'express';

const delete{{ properCase name }}Controller = async (req: Request, res: Response, next: NextFunction) => {
  try {
    res.send('Delete {{ properCase name }}');
  } catch (err) {
    next(err);
  }
};

export default delete{{ properCase name }}Controller;
