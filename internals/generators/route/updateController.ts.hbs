import { Request, Response, NextFunction } from 'express';
// import { bodySchema } from './validate';

const update{{ properCase name }}Controller = async (req: Request, res: Response, next: NextFunction) => {
  try {
    // await bodySchema.validate(req.body);
    res.send('Update {{ properCase name }}');
  } catch (err) {
    next(err);
  }
};

export default update{{ properCase name }}Controller;
